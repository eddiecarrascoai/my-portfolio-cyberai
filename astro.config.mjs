import { defineConfig } from 'astro/config';

export default defineConfig({
  // Replace with your actual GitHub Pages URL
  site: 'https://eddiecarrasco.github.io', 
  
  // If your repo is named exactly <your-username>.github.io, you can delete the 'base' line.
  // Otherwise, set it to your repository name exactly as it appears on GitHub:
  base: '/my-portfolio-cyberai', 
  
  // Keep the rest of the existing Devosfera config below...
});